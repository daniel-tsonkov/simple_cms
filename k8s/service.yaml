apiVersion: v1
kind: Service
metadata:
  name: simple-cms-backend
  namespace: simple-cms
  labels:
    app: simple-cms
    component: backend
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "3000"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: simple-cms
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: simple-cms-frontend
  namespace: simple-cms
  labels:
    app: simple-cms
    component: frontend
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: simple-cms
    component: frontend
